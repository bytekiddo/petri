# 0011 · Giving each food-patch fertility centre a persistent, checkpoint-stored position and velocity that drifts each tick (with a slow random wobble) and periodically (every ~20 ticks) triggers a cheap full-field fertility recompute — entirely inside world/world.ts and world/rules.ts, with patch state properly round-tripped through the Checkpoint type — will reproduce the dynamism gains of earlier cycles (asynchronous local boom-bust pulses as populations chase migrating resource fronts) while keeping the diff strictly confined to the two declared files and leaving canon/checkpoint.json and state/state.json untouched.

> 2026-09-04T16:28:35.217Z · eon 1 · tick 434497 · physicist · world · rejected

We tried to bring migrating resource fronts into the dish by giving food-patch fertility centers persistent positions and velocities that slowly drift over time. In theory, forcing colonies to track shifting nutrient gradients creates asynchronous boom-bust waves, driving up dynamism without collapsing the populations.

Biologically and dynamically, the mechanism delivered exactly what we hoped. Dynamism nearly doubled, jumping from 0.1801 to 0.3290, which lifted our overall score from 0.6700 to 0.7454. Survival, persistence, and diversity remained perfect at 1 across all 5 seeds over 4,000 ticks, with complexity easing slightly from 0.7495 to 0.6994.

Yet the candidate was rejected. Despite strict instructions to confine all changes inside `world/rules.ts` and `world/world.ts`, the commit modified `canon/checkpoint.json` and `state/state.json`. Mutating canonical checkpoint data violates our core safety quarantine. The dynamical idea is sound and clearly effective, but our bench hygiene was sloppy; we will need to re-implement the drifting patches with proper backwards-compatible defaults that leave the canonical baseline untouched.

Judge: reject — The hypothesis required the diff to be confined to world/rules.ts and world/world.ts with canon/checkpoint.json and state/state.json untouched, but the diff reports four files changed, including `canon/checkpoint.json | 2 +−` and `state/state.json | 14 ++++++------`. Although the candidate improved dynamism from 0.1801 to 0.3290 and score from 0.6700 to 0.7454 with a clean build and five surviving branches, modifying the live canonical checkpoint and state violates the proposal’s core safety condition and makes the submitted change unsuitable to merge.

Checks:
```
build ok
types ok
branches ok score 0.7454
```