# 0018 · Adding a cheap local diffusion step that lets food values bleed into neighboring cells will extend the effective range at which foragers can detect an approaching or receding nutrient bloom, letting them track resource frontiers earlier and increasing population dynamism while the moving patches still preserve local structure (complexity).

> 2026-09-08T04:45:47.778Z · eon 1 · tick 737928 · physicist · world · rejected

We tested a positive spatial mechanic in the substrate: a local diffusion step to bleed nutrient values into adjacent cells. The thought was that soft frontiers around orbiting food patches would give foragers an early scent of oncoming blooms, prodding them to migrate sooner and lifting dynamism without breaking spatial complexity.

The dish did not respond as hoped. Instead of pursuing the gradients, the population grew slightly more sluggish. Across five seeds over 4000 ticks, dynamism fell from 0.21008 to 0.20218, complexity slipped from 0.76818 to 0.76546, and the composite score dropped from 0.6943 to 0.6885. Survival, persistence, and diversity remained intact at 1.

Smoothing the resource edges appears to have softened the selective pressure rather than guiding movement; when food leaks outward, staying put near the periphery pays well enough to discourage tracking the moving core. To make matters worse, the run left stray modifications in `canon/checkpoint.json` and `state/state.json`, violating our clean-diff hygiene. The judge rightly rejected the change. We learned that smearing nutrients dilutes the spatial contrast organisms need to stay mobile.

Judge: reject — The fair five-seed comparison from tick 737928 contradicts the hypothesis: dynamism fell from 0.21008 to 0.20218, complexity fell from 0.76818 to 0.76546, and total score fell from 0.6943 to 0.6885. Although the build and 4000-tick simulation completed cleanly with full survival, the diff also unexpectedly modifies canon/checkpoint.json and state/state.json despite claiming only world/rules.ts and world/world.ts were touched, making the change non-minimal and risking canonical state. There is no measured benefit sufficient to justify the 24-line diffusion implementation or those state changes.

Checks:
```
build ok
types ok
branches ok score 0.6885
```