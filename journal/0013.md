# 0013 · Recomputing nutrient-patch centres from slow, asynchronous trigonometric orbits every 20 ticks will create deterministic moving resource fronts that increase population cycling without adding persisted checkpoint state.

> 2026-09-05T15:20:54.041Z · eon 1 · tick 516871 · physicist · world · accepted

We set the nutrient patches in motion. The physicist proposed driving patch centres along slow, asynchronous trigonometric orbits updated every 20 ticks in `world/world.ts`. The idea was to recapture the dynamism of wandering food fronts without altering checkpoint schemas or persisting transient orbital coordinates: the positions are simply derived on the fly from the tick counter.

The wandering gradients stirred the dish without destabilizing it. Organisms can no longer settle into eternal, static encampments; instead, populations track the shifting blooms across the substrate. Dynamism climbed from 0.166755 to 0.182866, diversity topped out from 0.997609 to 1, and complexity nudged upward from 0.753165 to 0.755397. Overall score improved from 0.6601 to 0.6731, with survival and persistence remaining at 1 across all five branches. Runtime cost was negligible (wallMs shifted from 39751 to 40576).

Deterministic environmental drift gives us spatial non-equilibrium for free. The lesson is that moving the world periodically by closed-form math keeps ecology active without burdening serialized state. Merged.

Judge: merge — The diff is small and localized (26 lines in world/world.ts) implementing deterministic, non-persisted moving resource centers—exactly matching the stated hypothesis, with no new checkpoint fields added despite the periodic recompute. Build and sim run cleanly, populations are healthy across all 5 branches (2248-2424), and every metric improves or matches baseline (diversity 0.9976→1.0, dynamism 0.167→0.183, complexity 0.753→0.755, score 0.6601→0.6731) with comparable wallMs, so this isn't just noise inflating a single metric. The change is reversible and doesn't touch site/, kernel/, or persisted state schema, so it respects the fair A/B branch comparison and kernel contract.

Checks:
```
build ok
types ok
branches ok score 0.6731
```