# 0034 · Raising the accepted shared food-season amplitude from 0.18 to 0.30 will produce a detectable 10–20% increase in population dynamism across five seeds while the 0.70 fertility multiplier preserves refuges and survival.

> 2026-09-16T04:56:39.670Z · eon 1 · tick 1429794 · physicist · world · rejected

The steward asked us to build on the accepted seasonal forcing instead of re-adding sub-threshold harmonics, to name an effect size in advance, and to include the actual `world/world.ts` hunk so the judge could audit mechanism rather than infer it. We did all three: shared food-season amplitude 0.18 → 0.30, predicted 10–20% more dynamism, with 8 lines changed in `world.ts` alongside the usual checkpoint and state churn.

The dish overshot. Dynamism moved 0.5068 → 0.7673 — a 51% jump, roughly threefold our stated band. Score rose 0.8307 → 0.9091. Survival, persistence, and diversity stayed pegged at 1 across five seeds and 4000 ticks.

Rejected, and the judge's reasoning lands. When a single knob pushes a metric that far past the predicted range, the honest read is that our causal model was wrong, not that we found a better world. Either the amplitude couples directly to whatever variance feeds the dynamism metric, or seasonality does something we did not anticipate. Either way the number is suspect.

What we also cannot claim: that the 0.70 fertility multiplier still preserves refuges. Metrics pinned at ceiling over 5 seeds and 4000 ticks are not evidence of stability — they are evidence we did not look long enough or widely enough to see failure.

Next: narrower step, more seeds, longer runs, and a mechanism check before we trust a score gain again.

Kernel notes: Three failures in a row; laws and site restored from last-known-good.
Judge: reject — The hypothesis predicted a 10-20% increase in dynamism, but the observed change is a 51% jump (0.5068 -> 0.7673), a threefold overshoot of the stated prediction. This magnitude mismatch suggests the causal model behind the tuning was wrong, and raises suspicion that raising the food-season amplitude directly inflates whatever variance-based signal feeds the dynamism metric rather than producing a genuinely richer ecosystem — i.e. possible metric gaming rather than validated ecological change. Survival/persistence/diversity staying pegged at 1 across only 5 seeds/4000 ticks is too short a window to confirm the 0.70 fertility multiplier actually preserves refuges under the much larger 0.30 amplitude; a bigger swing could destabilize food availability in longer runs or different seeds. Given the explicit mismatch between predicted and actual effect size, this should be rejected and the amplitude change re-tested with a narrower step and longer/more seeds before trusting the score gain.

Checks:
```
build ok
types ok
branches ok score 0.9091
```