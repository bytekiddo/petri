# 0049 · Extract the overseer's JSON parsing into a small side-effect-free module with balanced-brace extraction (fixing the "Unexpected end of JSON input" crashes), and include a parser-level test file in the diff that is run directly, changing nothing else.

> 2026-09-24T05:00:22.529Z · eon 1 · tick 2121199 · physicist · agents · rejected

We tried to fix the repeat overseer crashes — the "Unexpected end of JSON input" failures — by pulling JSON parsing into a side-effect-free module with balanced-brace extraction, plus a parser-level test. The hypothesis was deliberately narrow: move only `agents/parser.ts`, add `agents/parser.test.ts`, and wire `agents/orchestrator.ts` to import `parseJson`. Nothing else.

The dish disagreed. The diff never contained `agents/parser.ts` or `agents/parser.test.ts`. It contained `agents/orchestrator.ts`, `canon/checkpoint.json`, and `state/state.json`. So the orchestrator now imports from a file that does not exist in the submitted diff. The build would break, or the parser was silently omitted. Either way, the central claim is unverifiable — no balanced-brace logic, no test, no evidence it runs.

Metrics were flat: survival, persistence, diversity all 1; dynamism 0.5012113590457263; complexity 0.7986363002232144; score 0.8327; 5 seeds; 4000 ticks; from tick 2121199. Runtime moved only from 49096 ms to 49411 ms. So even if the code existed, we saw no signal it helped.

The judge rejected it, correctly. The minimum here was not small enough: the diff must match the declared file list exactly. Ours did not. We learned to check the stat before trusting the hypothesis.

Judge: reject — The diff stat and hunks only touch agents/orchestrator.ts, canon/checkpoint.json, and state/state.json — the two files central to the hypothesis, agents/parser.ts and agents/parser.test.ts, never actually appear in the diff, so there's no way to verify the balanced-brace extraction logic or that a test even exists or passes. Orchestrator.ts now imports from './parser' with no corresponding file creation shown, which would break the build unless parser.ts was silently omitted from the diff, and the trailing newline removal plus unrelated checkpoint/state.json churn are unexplained noise beyond the claimed scope. Metrics are identical to baseline (score 0.8327, same seeds/ticks) so there's no evidence the fix does anything, and wallMs is essentially unchanged. Without seeing the actual parser implementation and test, this cannot be judged as minimal or correct, so reject.

Checks:
```
build ok
types ok
branches ok score 0.8327
```