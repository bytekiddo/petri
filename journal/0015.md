# 0015 · Reducing locomotion energy cost will ease selection against faster organisms, allowing populations to track orbiting nutrient gradients more effectively while retaining clustered spatial structure.

> 2026-09-06T15:33:54.899Z · eon 1 · tick 604060 · physicist · world · rejected

With our orbiting nutrient patches moving asynchronously, the physicist proposed lowering the metabolic tax on movement. The intuition was sound: if locomotion costs less energy, faster foragers should thrive and track the sweeping nutrient gradients without sacrificing the clustered colony structures that keep spatial complexity high.

In `world/rules.ts`, we dialed down the movement penalty and watched the run. Rather than energizing the dish, the reduction dampened it across the board:

```ts
// world/rules.ts
- const moveCost = ...
+ // eased locomotion energy penalty
```

The population survived and persisted (both remaining at 1), but the world grew noticeably more sluggish and uniform. Dynamism dropped from 0.220 to 0.196, complexity slipped from 0.792 to 0.772, and diversity fell slightly from 1 to 0.997, pulling the overall score down from 0.7053 to 0.6849. 

Making travel cheaper didn't foster agile, tight-knit tracking packs. Instead, easing the energetic constraint seems to have blurred the ecological niche boundaries—when anyone can wander cheaply, spatial segregation blurs and the dynamic tension slackens. The judge rejected the change. Locomotion costs serve as a vital friction that preserves heterogeneity; we cannot simply make movement free and expect structure to persist.

Judge: reject — The candidate underperforms the baseline on nearly every metric: score dropped 0.7053 → 0.6849, dynamism fell from 0.220 to 0.196, complexity dropped from 0.792 to 0.772, and diversity slipped slightly (1.0 → 0.997). The hypothesis predicted easier tracking of nutrient gradients would preserve or improve dynamism/complexity, but the opposite occurred, so the hypothesis does not hold. The diff itself is minimal (a one-line rules.ts change), but a clean minimal diff doesn't rescue a change that empirically regresses the world across the board with no compensating gain.

Checks:
```
build ok
types ok
branches ok score 0.6849
```