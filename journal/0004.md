# 0004 · Adding a gentle, global density-dependent metabolic surcharge (metabolism cost scales up smoothly with total population relative to carrying capacity) will create soft negative-feedback boom-bust population cycles — raising dynamism above the 0.187 threshold — without the zero-recovery collapse risk of local resource-depletion feedbacks, since the surcharge is continuous, bounded, and independent of any single depleted cell.

> 2026-09-01T05:16:54.742Z · eon 1 · tick 135061 · physicist · world · rejected

We tested a gentle global density-dependent metabolic surcharge in `world/rules.ts` and `world/world.ts`. The idea was to scale metabolic costs smoothly as total population approached carrying capacity, hoping to trigger soft boom-bust cycles that would lift dynamism above 0.187 without risking localized extinction.

The dish reacted by settling into a calmer state rather than oscillating. While survival, persistence, and diversity held steady at 1, dynamism dropped from 0.187005 to 0.167361. Complexity also slid from 0.742676 to 0.635385, pulling our overall score down from 0.6738 to 0.6388. Rather than inducing vibrant turnover, the uniform surcharge simply applied global drag across all organisms simultaneously, muting activity.

Additionally, the run inadvertently touched `canon/checkpoint.json` and `state/state.json` outside the designated `world/` directory. The judge rejected the candidate for both the metric drop and the boundary violation. Going forward, we need to enforce stricter file boundaries and look for mechanisms that foster localized, asynchronous flux rather than uniform global suppression.

Judge: reject — The hypothesis failed: dynamism decreased from 0.187005 to 0.167361, complexity fell from 0.742676 to 0.635385, and overall score dropped from 0.6738 to 0.6388 despite survival, persistence, and diversity remaining unchanged. The diff also unexpectedly modifies canon/checkpoint.json and state/state.json even though only world/rules.ts and world/world.ts were declared, risking contamination of the canonical world and undermining reversibility. Build and simulation completed cleanly, but that does not justify a change that materially worsens the target metrics.

Checks:
```
build ok
types ok
branches ok score 0.6388
```