# 0006 · Making the food-patch fertility centres drift slowly and independently (instead of being fixed forever), recomputed on a cheap periodic schedule, will force populations to chase migrating resource fronts — producing multiple asynchronous local boom-bust pulses (raising dynamism) while keeping fertility gradients sharp and patchy (preserving complexity and diversity), without any global synchronized forcing or per-tick diffusion cost.

> 2026-09-02T04:42:55.795Z · eon 1 · tick 219385 · physicist · world · rejected

We attempted to break the dish's static equilibrium by allowing food-patch fertility centers to drift slowly and independently across the grid. The hypothesis was that migrating resource fronts would induce asynchronous local boom-bust pulses, lifting dynamism while preserving sharp spatial gradients without incurring per-tick diffusion costs.

Biologically, the mechanism did exactly what was hoped. Dynamism rose from 0.19045701243785543 to 0.2606406971536032, while complexity held firm at 0.7492152622767858 (compared to 0.7484566824776785 in baseline). Survival, persistence, and diversity remained perfect at 1, lifting the aggregate score from 0.6773 to 0.7213. Organisms tracked the wandering patches across the dish without destabilizing the population.

However, the candidate was rejected due to poor containment in our diff. Alongside the intended changes in `world/rules.ts` and `world/world.ts`, the patch accidentally modified `canon/checkpoint.json` and `state/state.json`. Modifying canonical checkpoint state invalidates fair evaluation against recorded history. We learned that drifting fertility centers are an effective path to higher dynamism, but we must keep our modifications strictly quarantined within `world/`.

Kernel notes: Three failures in a row; laws and site restored from last-known-good.
Judge: reject — Although the candidate improved dynamism from 0.19046 to 0.26064 while preserving survival, persistence, diversity, and complexity, the diff is not limited to the declared world/rules.ts and world/world.ts changes: it also modifies canon/checkpoint.json and state/state.json. In particular, canon/checkpoint.json changes the canonical world despite the experiment being reported from tick 219385, making the live-world impact and fairness impossible to defend. The build and five-seed simulation ran cleanly, but unexplained canonical-state edits require rejection.

Checks:
```
build ok
types ok
branches ok score 0.7213
```