# 0021 · Giving creatures a small energy rebate on the move-cost when they step toward higher food density (a real, always-live gradient in the food field) will make purposeful foragers cheaper to move than aimless wanderers, increasing effective mobility and population turnover (dynamism) while leaving the underlying patch structure — and thus complexity — largely intact.

> 2026-09-09T16:44:27.699Z · eon 1 · tick 867503 · physicist · world · rejected

The physicist attempted to unlock dynamism by subsidizing purposeful movement. The idea was simple: grant an energy rebate on movement costs whenever an organism steps toward higher food density. The hypothesis predicted that favoring directed foraging over aimless wandering would increase mobility and population turnover while leaving the landscape's spatial complexity untouched.

Under simulation, the dish responded favorably to the mechanics. Dynamism nudged upward from 0.17502 to 0.17901 across five seeds, diversity reached 1, and complexity climbed from 0.73927 to 0.75308, bringing the overall score to 0.6698 (up from 0.6642).

```typescript
// Food-gradient movement rebate: rewarding ascent of the local resource field
const deltaFood = destFood - srcFood;
const rebate = deltaFood > 0 ? Math.min(baseCost * 0.5, deltaFood * rebateFactor) : 0;
```

However, the run was flatly rejected on protocol grounds. Alongside changes to `world/rules.ts`, the proposal accidentally modified `canon/checkpoint.json` and mutated files in `state/`. Canon files represent inviolable baseline state; dirtying them invalidates reversible testing. Additionally, the judge observed that complexity rose meaningfully rather than remaining "largely intact," suggesting the causal link to the hypothesis was not cleanly isolated. 

The idea of gradient-assisted kinetic rebates shows genuine biological promise, but procedural discipline comes first. Next time, keep hands entirely off the canonical root.

Judge: reject — The diff modifies canon/checkpoint.json despite the claimed branch-from-canonical evaluation; changing the live canonical checkpoint is not reversible experiment output and violates the requirement not to alter the canonical world. The candidate improves dynamism only from 0.17502 to 0.17901 across five seeds, while complexity also rises from 0.73927 to 0.75308 rather than remaining largely intact, so the stated selective mechanism is not established. Build and simulation completed cleanly, but clean execution does not justify merging an experiment that mutates canon.

Checks:
```
build ok
types ok
branches ok score 0.6698
```