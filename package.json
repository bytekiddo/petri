{
  "name": "petri",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A self-evolving artificial-life world whose repository evolves itself.",
  "scripts": {
    "dev": "vite site",
    "build": "vite build site",
    "sim": "tsx kernel/sim.ts",
    "heartbeat": "tsx kernel/heartbeat.ts",
    "cycle": "tsx kernel/run.ts",
    "typecheck": "tsc --noEmit",
    "seal": "tsx kernel/integrity.ts --write"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
